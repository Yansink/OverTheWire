A program is running automatically at regular intervals from cron, the time-based job scheduler. Look in /etc/cron.d/ for the configuration and see what command is being executed.

This level covers the structure of cron, the tool for automating script execution.

The configuration is located in /etc/cron.d
To view the contents try to ls, there is so many file. But keep focus on the next level, which bandit22.
let's try to break down the file cronjob_bandit22, and see what's going on.

            * * * * * bandit22  &> /dev/null
what this all mean?
see the 5 asterisks, starting from the left, they are the minute, hour, date, month, and day. The user is bandit22,
and this file is, what shoud cron execute letter /usr/bin/cronjob_bandit22.sh.


| Cron Expression | Meaning                                  | //  ***** mean every possible value. Every minute, hour, day of the mounth, every mounth
| --------------- | ---------------------------------------- |           and every day of the week
| `* * * * *`     | Every minute                             |  
| `*/5 * * * *`   | Every 5 minutes (0, 5, 10, ..., 55)      |
| `0 * * * *`     | At minute 0 of every hour                |
| `0 2 * * *`     | Every day at 2:00 AM                     |
| `30 8 * * 1`    | Every Monday at 8:30 AM                  |
| `0 0 1 * *`     | Midnight on the first day of every month |



let's break down the file crobjob_bandit22.sh
open the file, use cat or nano. 
        
          #!/bin/bash
          chmod 644 /tmp/t7O6lds9S0RqQh9aMcz6ShpAoZKF7fgv
          cat /etc/bandit_pass/bandit22 > /tmp/t7O6lds9S0RqQh9aMcz6ShpAoZKF7fgv

When the time comes, cron will update the permissions and overwrite the file `/tmp/t7O6lds9S0RqQh9aMcz6ShpAoZKF7fgv` with the contents of the file `/etc/bandit_pass/bandit22`.

Okey, now find the password to the next level, just open the file bandit22 or /tmp/t7O6lds9S0RqQh9aMcz6ShpAoZKF7fgv

      PASSWORD SOLVED [ RYVux2rHEm9tiXHmLFzuR7Vhx6AZQMEz ]
          
            
