🚩 The password for the next level can be retrieved by submitting the password of the current level to port 30001 on localhost using SSL/TLS encryption.

Helpful note: Getting “DONE”, “RENEGOTIATING” or “KEYUPDATE”? Read the “CONNECTED COMMANDS” section in the manpage.

🗒️ command learned:
ssh, telnet, nc, ncat, socat, openssl, s_client, nmap, netstat, ss

1. Log in to the level.
2. openssl s_client command used to make a scure connection
3. use -connect option or -host, but in this case i prevere use -connect.
4. Input the current password, enter. Password solved 🔑 [ kS0Hf0u5HiXFwKMKFqXvPdOTNGGa0X8V ] 
